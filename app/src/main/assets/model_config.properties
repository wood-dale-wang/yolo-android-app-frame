# Model file names loaded by app at runtime
# Update these keys when changing exported ncnn model files.
model.param=yolov8s_lic.param
model.bin=yolov8s_lic.bin
labels.path=labels.txt
# Optional: custom model name shown in UI; leave empty to infer from model.param
model.display_name=
